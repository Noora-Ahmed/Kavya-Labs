/** @type {import('tailwindcss').Config} */
module.exports = {
    content: ["./src/**/*.{js,jsx,ts,tsx}"], // ✅ scans all React files
    theme: {
      extend: {}, // place custom colors, fonts, spacing here later
    },
    plugins: [], // keep empty unless you add official plugins
  };
  